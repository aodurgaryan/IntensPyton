#include <stdio.h>

int main() {
    // Так как ABCD — параллелограмм, оно имеет равные противоположные стороны и углы. Таким образом, длины сторон AB и CD равны
    //Когда точка E делит диагональ BD пополам, треугольники ABE и ECD имеют одинаковую высоту, поскольку они опираются на одну и ту же прямую, проходящую через точку E на линии, параллельной AD и BC, что подтверждает равенство высот.
    //Следовательно, отношение площадей этих треугольников будет равно отношению длин оснований а значит будет 1
    double S_ABE = 1.0;  // Площадь треугольника ABE
    double S_ECD = 1.0;  // Площадь треугольника ECD
    double otnoshenie ;
    otnoshenie = S_ABE/S_ECD;
    printf("Отношение площадей треугольников ABE и ECD: %.2f\n",otnoshenie );

    return 0;
}
