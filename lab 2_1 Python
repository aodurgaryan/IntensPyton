print("Дано целое число. Определить, кратно ли произведение его цифр заданному числу а.")
a = int(input("Введите целое число а: "))
n = int(input("Введите целое число n: "))
# чтобы выяислить произведение цифр числа n я перевожу его в строковый вид для удобства
product = 1
for digit in str(abs(n)):  # Используем abs для обработки отрицательных чисел
    product *= int(digit)

# Проверяем, кратно ли произведение a
if product % a == 0:
    print("Произведение цифр числа n кратно a.")
else:
    print("Произведение цифр числа n не кратно a.")
