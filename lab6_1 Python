# Найти сумму чисел от 100 до 200, кратных 17.
sum_numbers = 0
# Начальное значение
number = 100
# Цикл с постусловием
while True:
    if number > 200:  # Проверка условия выхода из цикла
        break
    if number % 17 == 0:  # Проверка кратности 17
        sum_numbers += number  # Добавление к сумме
    number += 1  # Переход к следующему числу

# Вывод результата
print("Сумма чисел от 100 до 200, кратных 17:", sum_numbers)
