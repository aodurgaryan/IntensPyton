//Напечатать те из двузначных чисел, которые делятся на 4, но не делятся на 6.
#include <stdio.h>

int main() {
    // Перебираем все двузначные числа от 10 до 99
    for (int i = 10; i < 100; i++) {
        // Проверяем, делится ли число на 4 и не делится на 6
        if (i % 4 == 0 && i % 6 != 0) {
            printf("%d\n", i); // Печатаем число, если условия выполнены
        }
    }
    return 0;
}
