# Составьте программу вычисления суммы всех двузначных чисел
sum_of_numbers = 0
# Начальное значение
number = 10
# Цикл с предусловием
while number < 100:
    sum_of_numbers += number  # Добавляем текущее число к сумме
    number += 1  # Переходим к следующему числу
print("Сумма всех двузначных чисел:", sum_of_numbers)
